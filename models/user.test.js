const User = require('./user')

describe('User', () => {
  beforeAll(async () => {
    await User.deleteMany()
  })

  test('TODO:', async () => {
    //
  })

  test('caching', async () => {
    // TODO:
  })
})
